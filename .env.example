# Used locally by `npm run build:index`.
OPENAI_API_KEY=

# Used at runtime by the MCP search tool through Vercel AI Gateway.
AI_GATEWAY_API_KEY=

# Required only when `npm run upload:index` uploads the private index to Vercel Blob.
BLOB_READ_WRITE_TOKEN=
