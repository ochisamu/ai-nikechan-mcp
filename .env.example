# Used locally by `npm run build:index` and at runtime by the MCP search tool.
OPENAI_API_KEY=

# Required only when `npm run upload:index` uploads the private index to Vercel Blob.
BLOB_READ_WRITE_TOKEN=
